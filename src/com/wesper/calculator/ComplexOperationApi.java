package com.wesper.calculator;

public interface ComplexOperationApi {

    double sqrt(int number);

    double sqrt(double number);

    double pow(int number, int power);

    double pow(double number, int power);
}
